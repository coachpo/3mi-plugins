interface:
  display_name: "Parallel Repository Research"
  short_description: "并行只读拆分代码库调研并汇总可核验证据"
  default_prompt: "使用 $steward:parallel-repository-research 将复杂仓库调研拆分为独立只读搜索 lane，并汇总带路径证据的结果。"

policy:
  allow_implicit_invocation: true
