interface:
  display_name: "Parallel Repository Research"
  short_description: "并行只读拆分代码库调研并汇总可核验证据"
  default_prompt: "使用 $steward:parallel-repository-research 并行调查至少两个有用的独立仓库问题，由主代理复核源码证据后回答，并说明影响结论的冲突和调查缺口。"

policy:
  allow_implicit_invocation: true
