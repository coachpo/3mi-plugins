interface:
  display_name: "Closed-loop GOAL Verification"
  short_description: "按别名绑定验收计划并闭环验证当前工作树 GOAL"
  default_prompt: "使用 $steward:run-closed-loop-verification 验收当前工作树中指定别名的 GOAL，冻结执行绑定并完成修补、定向复测与收尾核验。"
policy:
  allow_implicit_invocation: false
