interface:
  display_name: "GOAL Verification and Repair"
  short_description: "Verify completed GOALs and repair scoped defects"
  default_prompt: "使用 $steward:run-closed-loop-verification 根据当前工作树验收 .steward/goal.txt，在 GOAL 范围内原地修补问题并复验。"
policy:
  allow_implicit_invocation: false
