interface:
  display_name: "Engineering Control Loop"
  short_description: "运行从 GOAL、profiles 到同源回归审计的工程闭环"
  default_prompt: "使用 $steward:run-engineering-control-loop，把主会话已解析的唯一目标 worktree 根绑定到这个已授权工程闭环，并运行到 request-bound Review、同源完整回归和 audit。"

policy:
  allow_implicit_invocation: false
