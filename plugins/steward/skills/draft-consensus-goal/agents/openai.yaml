interface:
  display_name: "Draft Consensus Goal"
  short_description: "绑定目标 worktree 并起草七行中文 GOAL"
  default_prompt: "使用 $steward:draft-consensus-goal，消费主会话已解析的唯一目标 worktree 根，在该精确工作树核实事实并起草七行中文 GOAL；按契约处理交接文件，不要读取宿主 Goal 或执行状态。"

policy:
  allow_implicit_invocation: false
